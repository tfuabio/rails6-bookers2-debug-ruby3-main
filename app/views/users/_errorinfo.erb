<% if errorinfo.errors.any? %>
  <%= errorinfo.errors.count %> errors prohibited this user from being saved:
  <ul>
    <% errorinfo.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>